<h1> <%= comedian.name %></h1>
<a href="/comedians" id="new_link">Return to previous screen</a><br>

<div class="one_comedian">
  <h3> <%= comedian.age %> years old</h3>
  <h3>Born in <%= comedian.city %></h3>
</div>

  <h2 id="special">Specials:</h2>
<div class="one_special">
<% comedian.specials.each do |special| %>
    <h4><%= special.name %></h4>
    <h4><%= special.length %> minutes<h4>
    <img src=<%= special.url %>>
  <% end %>
</div>
