<!DOCTYPE=html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="stylesheets/main.css" title="css" type="text/css">
  </head>
  <body>
    <h1>Comedians</h1>
    <section id="statistics">
      <h3>Statistics</h3>
      <section id="average_age">
        <h5>Average Age</h5>
        <p><%= @comedians.average_age %> years old</p>
      </section>

    </section>
    <% @comedians.each do |comedian| %>
      <ul id="comedian-<%= comedian.id %>" class="comedian-box">
        <li>Name: <%= comedian.name %></li>
        <li>Age: <%= comedian.age %></li>
        <li>Hometown: <%= comedian.hometown %></li>
        <hX>Specials:</hX><br/>
        <% comedian.specials.each do |special| %>
        <img src="<%= special.image_file %>" alt="<%= special.title %>" class="image-special"/>
          <ul id="special-<%= special.id %>" class="special-info">
            <li>Title: <%= special.title %></li>
            <li>Runtime: <%= special.runtime %> min.</li>
          </ul>
        <% end %>
      </ul>
    <% end %>
  </body>
</html>
